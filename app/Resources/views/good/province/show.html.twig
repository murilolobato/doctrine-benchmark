{% extends 'base.html.twig' %}

{% block body %}
    <h1>Province</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ province.id }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ province.abbreviation }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ province.name }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if province.createdAt %}{{ province.createdAt|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('web_good_province_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('web_good_province_edit', { 'id': province.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
